ANTHROPIC_API_KEY=sk-ant-your-key-here
PORT=3001

# Intake bridge (server-side only — do NOT prefix with VITE_).
# Set in Vercel Project Settings → Environment Variables (Production + Preview).
# Must match the corresponding values in toothdoc1978-bit/dental-intake-forms.
INTAKE_MIDDLEWARE_URL=https://intake-middleware.vercel.app
INTAKE_SHARED_SECRET=replace-with-secret-shared-with-dental-intake-forms
