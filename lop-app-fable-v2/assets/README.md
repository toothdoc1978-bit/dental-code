# assets/

`lop_map.jpg` is the Lookout Point aerial used as the pan/zoom reference map.
The app loads `assets/lop_map.jpg`; if it's missing it shows a placeholder
(no crash). Replace it with an updated aerial any time — same filename.
